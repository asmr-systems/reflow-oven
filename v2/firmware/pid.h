#ifndef REFLOW_PID_H
#define REFLOW_PID_H


class PID {

    PID() {}

    void update(double set_point, double current_temp) {
        // output the duty cycle for pulsing the power. 0 is off
    }
};

#endif
